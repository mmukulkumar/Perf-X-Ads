# ===========================================
# PERF X ADS - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values

# -------------------------------------------
# SUPABASE CONFIGURATION
# -------------------------------------------
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# -------------------------------------------
# STRIPE CONFIGURATION
# -------------------------------------------
# Get these from: https://dashboard.stripe.com/apikeys
# Use test keys for development, live keys for production

# Publishable Key (starts with pk_test_ or pk_live_)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key

# -------------------------------------------
# STRIPE PRICE IDS
# -------------------------------------------
# Create products and prices in Stripe Dashboard:
# https://dashboard.stripe.com/products
#
# Monthly Subscription Price ID
VITE_STRIPE_PRICE_MONTHLY=price_monthly_id

# Annual Subscription Price ID
VITE_STRIPE_PRICE_ANNUAL=price_annual_id

# Lifetime (One-time) Price ID - Solo
VITE_STRIPE_PRICE_LIFETIME=price_lifetime_solo_id

# Lifetime (One-time) Price ID - Team (5 users)
VITE_STRIPE_PRICE_LIFETIME_TEAM=price_lifetime_team_id

# -------------------------------------------
# GOOGLE AI (GEMINI) API KEY
# -------------------------------------------
# Get from: https://aistudio.google.com/app/apikey
VITE_API_KEY=your-google-ai-api-key

# ===========================================
# SUPABASE EDGE FUNCTION SECRETS
# ===========================================
# These are set via Supabase CLI, not in .env file
# Run: supabase secrets set STRIPE_SECRET_KEY=sk_test_xxx
#
# STRIPE_SECRET_KEY=sk_test_your_secret_key
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
#
# The webhook secret is obtained when you create a webhook endpoint
# in Stripe Dashboard: https://dashboard.stripe.com/webhooks
# Endpoint URL: https://your-project.supabase.co/functions/v1/stripe-webhook

# ===========================================
# STRIPE SETUP INSTRUCTIONS
# ===========================================
#
# 1. Create a Stripe account at https://stripe.com
#
# 2. Create Products in Stripe Dashboard:
#    - Go to Products > Add Product
#    - Create "Pro Monthly" with recurring price ($29/month)
#    - Create "Pro Annual" with recurring price ($180/year or $15/month billed annually)
#    - Create "Lifetime Solo" with one-time price ($249)
#    - Create "Lifetime Team" with one-time price ($1,890)
#
# 3. Copy the Price IDs (starts with price_) to this file
#
# 4. Deploy Supabase Edge Functions:
#    supabase functions deploy create-checkout-session
#    supabase functions deploy create-portal-session
#    supabase functions deploy stripe-webhook
#
# 5. Set Supabase secrets:
#    supabase secrets set STRIPE_SECRET_KEY=sk_test_xxx
#    supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_xxx
#
# 6. Create Webhook in Stripe Dashboard:
#    - Go to Developers > Webhooks > Add endpoint
#    - URL: https://your-project.supabase.co/functions/v1/stripe-webhook
#    - Events to listen for:
#      * checkout.session.completed
#      * customer.subscription.created
#      * customer.subscription.updated
#      * customer.subscription.deleted
#      * invoice.payment_succeeded
#      * invoice.payment_failed
#
# 7. Copy the webhook signing secret to Supabase secrets
